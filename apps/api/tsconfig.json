{
  "compilerOptions": {
    "module": "CommonJS",
    "moduleResolution": "Node",
    "target": "ES2022",
    "lib": ["ES2022"],
    "declaration": false,
    "removeComments": false,
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "sourceMap": true,
    "outDir": "./dist",
    // Pinned so the emitted tree is dist/main.js rather than dist/src/main.js.
    // prisma/seed.ts is deliberately outside this root — it runs via tsx and is
    // type-checked by tsconfig.seed.json.
    "rootDir": "./src",
    "baseUrl": "./",
    "incremental": true,
    // The build info MUST live inside outDir. `nest build` sets deleteOutDir, so a
    // build-info file kept outside dist survives the wipe, convinces tsc that the
    // already-deleted outputs are current, and produces a dist missing most of its
    // files — which only fails at runtime, long after a green build.
    "tsBuildInfoFile": "./dist/.tsbuildinfo",
    "skipLibCheck": true,
    "strict": true,
    "strictNullChecks": true,
    "strictPropertyInitialization": false,
    "noImplicitAny": true,
    "noUnusedLocals": false,
    "forceConsistentCasingInFileNames": true,
    "noFallthroughCasesInSwitch": true,
    "resolveJsonModule": true,
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "test", "src/**/*.spec.ts"]
}
